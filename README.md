# Buka CLI

Buka CLI is a general-purpose plugin-based CLI.

The CLI app is on `rust` directory. Why? Because currently the CLI is written in Rust. Maybe in future it will be rewritten in another language, then we will have other directory for that language.